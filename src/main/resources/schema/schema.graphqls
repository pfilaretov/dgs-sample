type Query {
    shows(title: String, role: String): [Show]
}

type Show {
    title: String
    releaseYear: Int
    actors: [Actor]
}

type Actor {
    firstName: String
    lastName: String
    role: String
}

